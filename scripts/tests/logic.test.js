const test = require('tape');
const anyName = require("../logic.js");

test("Test 1", function(t){
  var actual = {};
  var expected = {};
  t.equal(atctual , expected , 'Two values must be equal');
  t.end();
});
